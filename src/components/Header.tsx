"use client";

// Removed useTheme import

const Header = () => {
  // Removed useTheme hook call

  return (
    <h1 className={`text-4xl font-bold mb-4 text-white`}>🤖 ChatTots</h1>
  );
};

export default Header;